task:
  container:
    image: gcc:latest
  build_script:
    - sh build.sh _ 
  amd64_artifacts:
    path: "bin/*"

task:
  arm_container:
    image: gcc:latest
  build_script:
    - sh build.sh _
  arm64_artifacts:
    path: "bin/*"